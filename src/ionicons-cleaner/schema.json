{
    "$schema": "http://json-schema.org/schema",
    "$id": "ioniconsCleaner",
    "title": "ionicons tree shaking schema",
    "type": "object",
    "description": "Remove not used icons",
    "properties": {
      "output-path": {
        "type": "string",
        "description": "outputPath of the build",
        "default": "www"
      },
      "svg-dir": {
        "type": "string",
        "description": "Relative path of the svg folder from the outputPath",
        "default": "svg"
      },
      "sw-svg": {
        "type": "string",
        "description": "svg name in the sw assetGroups",
        "default": "svg"
      },
      "force-delete": {
        "type": "boolean",
        "description": "Force delete even if you get dynamic warning",
        "default": false
      },
      "whitelist": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "List of icons that will not delete"
      }
    }
   }